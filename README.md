**⚡ Sirius Achievements**

****⚡ Sirius Achievements**
** — это современная стартовая админ-панель для веб\-приложений, написанная на **Python (FastAPI)** с использованием шаблонизатора **Jinja2** и **Bootstrap 5**.

Проект предоставляет готовую архитектуру для управления пользователями, ролями, загрузки файлов с предварительной обработкой (кадрированием) и поддержки мультиязычности.

## **✨ Основные возможности**

* **🔐 Аутентификация и Авторизация**: Вход по Email/Паролю, сессии, JWT токены для API.  
* **👥 Управление пользователями**: CRUD операции, назначение ролей (Admin, Moderator, Student, Guest), бан/активация.  
* **🖼️ Загрузка файлов**:  
  * Загрузка изображений (аватарок и документов/достижений).  
  * **Встроенный редактор (Cropping)**: Возможность обрезать изображение перед загрузкой на сервер (используется Cropper.js).  
  * Асинхронное сохранение файлов на диск.  
* **🌍 Мультиязычность (i18n)**:  
  * Поддержка перевода интерфейса (RU/EN).  
  * Переключение языка сохраняется в сессии.  
  * Оптимизированная загрузка переводов (Singleton).  
* **🛡️ Модерация**: Отдельный раздел для проверки загруженных достижений и регистрации новых пользователей.  
* **⚡ Технологии**: Асинхронный код (async/await), SQLAlchemy ORM, Alembic для миграций.

## **🛠️ Стек технологий**

* **Backend**: Python 3.10+, FastAPI, Starlette, Uvicorn.  
* **Database**: SQLAlchemy (ORM), Alembic (миграции). Поддержка PostgreSQL (рекомендуется), SQLite.  
* **Frontend**:  
  * HTML5 \+ Jinja2 (Server Side Rendering).  
  * **Bootstrap 5** (подключен через CDN для быстрой работы).  
  * **Cropper.js** (для кадрирования изображений).  
  * FontAwesome 4.7 (иконки).

**🚀 Установка и запуск**

### **1\. Клонирование репозитория**

git clone https://github.com/RochDM231718/app.git/asyn 

### **2\. Настройка окружения**

Создайте виртуальное окружение и активируйте его:

\# Windows  
python \-m venv .venv  
.venv\\Scripts\\activate

\# Linux/macOS  
python3 \-m venv .venv  
source .venv/bin/activate  
Установите зависимости:

pip install \-r requirements.txt

### **3\. Конфигурация (.env)**

Создайте файл .env на основе примера:

cp .env.example .env

Отредактируйте .env, указав параметры вашей базы данных (по умолчанию настроено на локальный PostgreSQL):

DB\_DRIVER\=postgres  
DB\_HOST\=127.0.0.1  
DB\_PORT\=5432  
DB\_NAME\=sirius\_db  
DB\_USERNAME\=your\_user  
DB\_PASSWORD\=your\_password

\# ВАЖНО: Смените секретные ключи для продакшена\!  
ADMIN\_SECRET\_KEY\=сгенерируйте\_длинный\_случайный\_ключ  
API\_SECRET\_KEY\=сгенерируйте\_другой\_ключ

### 

### **4\. База данных и Миграции**

Убедитесь, что PostgreSQL запущен и база данных создана. Затем примените миграции:

alembic upgrade head

### **5\. Заполнение начальными данными (Seeding)**

Чтобы создать дефолтного администратора и тестовых пользователей, выполните команду (через CLI утилиту):

python cli.py seed

### **6\. Запуск сервера**

Запустите сервер разработки с авто-перезагрузкой:

uvicorn main:app \--reload \--env-file .env

Админка будет доступна по адресу: [http://127.0.0.1:8000/admin](https://www.google.com/search?q=http://127.0.0.1:8000/admin)

**📂 Структура проекта**

appp/  
├── app/  
│   ├── infrastructure/  \# База данных, переводы, работа со статикой, JWT  
│   ├── middlewares/     \# Middleware (аутентификация, контекст)  
│   ├── migrations/      \# Миграции Alembic  
│   ├── models/          \# SQLAlchemy модели  ыы
│   ├── repositories/    \# Слой доступа к данным (CRUD)  
│   ├── routers/         \# Маршруты (Admin и API)  
│   ├── schemas/         \# Pydantic схемы (валидация данных)  
│   ├── services/        \# Бизнес-логика  
│   └── tests/           \# Тесты  
├── static/              \# CSS, JS, шрифты и загруженные файлы (uploads)  
├── templates/           \# HTML шаблоны (Jinja2)  
├── translations/        \# JSON файлы локализации (ru.json, en.json)  
├── tools/               \# Утилиты CLI и скрипты  
├── main.py              \# Точка входа приложения  
├── cli.py               \# Командный интерфейс  
└── requirements.txt     \# Зависимости  
